{
  "name": "oflow-workflow",
  "version": "0.1.0",
  "description": "A small, GitLab-first workflow layer for Claude, Codex, Copilot, and VS Code agents.",
  "type": "module",
  "bin": {
    "oflow": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('node:fs').chmodSync('dist/cli.js', 0o755)\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check:public": "node scripts/check-public-content.mjs",
    "test": "npm run check:public && npm run build && node --test test/*.test.mjs",
    "prepack": "npm run check:public && npm run build",
    "prepublishOnly": "npm run check:public && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/PolderLabsVOF/oflow.git"
  },
  "bugs": {
    "url": "https://github.com/PolderLabsVOF/oflow/issues"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.0",
    "typescript": "^7.0.2"
  }
}
